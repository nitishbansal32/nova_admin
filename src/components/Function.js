import React from "react";
import { Typography, Box } from "@mui/material";

const Functions = () => {
  return (
    <Box sx={{ padding: 3 }}>
      <Typography variant="h4" gutterBottom>
        Functions
      </Typography>
      <Typography variant="body1">In dev</Typography>
    </Box>
  );
};

export default Functions;
